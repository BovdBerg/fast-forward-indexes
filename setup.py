from setuptools import setup

setup(
    name='fast_forward',
    version='0.3',
    packages=['fast_forward'],  # Explicitly specify the 'fast_forward' package
    # Other arguments like author, description, etc.
)
